@model ProteinManagementSystem.Web.Models.ProteinViewModel

@{
    ViewBag.Title = "Edit";
}

@Html.Partial("_CreateOrEditProteinForm", Model,
         new ViewDataDictionary(ViewData) { { "header", "Protein" }, { "action", "Edit" } }
        )